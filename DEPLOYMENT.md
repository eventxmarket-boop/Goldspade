# Deployment Notes

## Release rule

- Make local changes first.
- Sync the updated workspace to the server and overwrite the previous version.
- Report both the local version and the server version after each release.

## Versioning

- Versioning starts at `1.0.1`.
- Versions must increase monotonically.
- Do not rename or skip versions.
